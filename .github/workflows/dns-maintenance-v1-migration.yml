name: DNS maintenance v1 migration

on:
  workflow_dispatch:
    inputs:
      apply_migration:
        description: "Apply v1 migration and commit managed DNS state"
        required: true
        default: false
        type: boolean

permissions:
  contents: write

concurrency:
  group: dns-maintenance-telegram-${{ github.ref }}
  cancel-in-progress: false

jobs:
  maintain:
    if: ${{ inputs.apply_migration }}
    uses: DKHNV/DNS-Maintenance/.github/workflows/reusable-maintenance.yml@c8696805dbdafe8e368b394dc5f21097455352c5
    with:
      config_path: dns-maintenance-v1.json
      collection: telegram
      dry_run: false
      engine_ref: c8696805dbdafe8e368b394dc5f21097455352c5
